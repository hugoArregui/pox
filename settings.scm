(define db-connection-spec
  '((host   . "localhost")
    (dbname . "pox")
    (user   . "hugo")))

;(use pox-mail pox-mail/sendmail)

;(sendmail-program "/usr/bin/msmtp -t -f foo@bar")
;(send-mail-with sendmail-send)
